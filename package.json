{
  "name": "shriram-clothings",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd noren-messaging-frontend && npm install --legacy-peer-deps && npm run build",
    "vercel-build": "cd noren-messaging-frontend && npm install --legacy-peer-deps && npm run build"
  }
}
